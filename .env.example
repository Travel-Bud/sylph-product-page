# Canonical public origin of this site; used for metadataBase, canonical links and OG image URLs.
NEXT_PUBLIC_SITE_URL=https://sylph-product.com
# Endpoint the demo form posts leads to (the Cloudflare email-worker in sylph-infra).
NEXT_PUBLIC_EMAIL_API_URL=https://<email-worker-host>/
